package net.trickycreations.trickytpa.tasks;

/**
 * No longer used — expiration is handled inline in TpaManager
 * via Bukkit.getGlobalRegionScheduler().runDelayed().
 *
 * Kept as a stub in case external code references the class.
 * Remove if you see this in a full cleanup pass.
 */
public final class TpaRequestRunnable {
    private TpaRequestRunnable() {}
}
